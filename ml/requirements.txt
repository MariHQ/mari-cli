# Pinned to a coherent set: gliner 0.2.x is built on transformers 4.x, so transformers
# stays <5 (the v4 API is what mari_ml.py uses, e.g. torch_dtype=).
torch>=2.2,<3
transformers>=4.40,<5
gliner>=0.2.13,<0.3
sentencepiece>=0.2
protobuf>=4
accelerate>=0.30
