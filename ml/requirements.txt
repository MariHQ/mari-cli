# transformers 5.6.x is the sweet spot: new enough to recognize the default perplexity
# model's architecture (Qwen3.5 → qwen3_5, added in 5.x) and still within gliner 0.2.x's
# declared bound (transformers<5.7,>=4.51.3), so pip resolves cleanly with no conflict.
torch>=2.2
transformers>=5.6,<5.7
gliner>=0.2.13,<0.3
sentencepiece>=0.2
protobuf>=4
accelerate>=0.30
